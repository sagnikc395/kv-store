[project]
name = "kv-store"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "grpcio>=1.80.0",
    "protobuf<4.0.0",
    "pytest>=9.0.3",
]
